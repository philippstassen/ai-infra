# Database Migrations

Future SQL migrations for runtime schemas belong here.

Use this for explicit schema evolution after the initial Docker init scripts. Runtime state, run tracking, checkpoint tables, and promoted knowledge tables should be migrated here instead of being hidden in application startup code.
