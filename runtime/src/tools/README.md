# Tool Clients

Runtime tool clients wrap domain services and external integrations with typed inputs, side-effect classification, and permission checks.

Tools should be explicit and auditable. The starter scope allows carshare domain writes through configured permission classes and does not include GitHub write tools.
