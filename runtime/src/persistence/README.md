# Persistence

Database access for starter runtime channel bindings, sessions, messages, runs, and tool invocation audit.

Checkpoint, artifact, approval, handoff, and promoted-knowledge tables are intentionally out of starter scope.

Durable domain truth should remain in domain schemas such as `carshare`, not generic transcript memory.
