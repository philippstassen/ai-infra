# LangGraph Runtime

Graph loading, state schemas, synchronous execution, and reusable node helpers belong here.

Agent systems define what to run; this starter layer defines how graph runs are executed per message. Checkpoint/resume support is intentionally deferred.
