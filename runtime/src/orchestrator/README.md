# Orchestrator

The orchestrator resolves channel bindings, starts per-message runs, and connects inbound events to starter LangGraph workflows.

It owns run lifecycle coordination, not domain-specific behavior.
